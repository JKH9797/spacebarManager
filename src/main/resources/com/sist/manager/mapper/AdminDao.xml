<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sist.manager.dao.AdminDao">

	<resultMap type="com.sist.manager.model.Admin" id="adminResultMap">
		<id column="ADMIN_ID" property="adminId" />
		<result column="ADMIN_PWD" property="adminPwd" />
		<result column="ADMIN_NAME" property="adminName" />
		<result column="ADMIN_STAT" property="adminStat" />
		<result column="REG_DT" property="regDt" />	
	</resultMap>

	<select id="adminSelect" parameterType="string" resultMap="adminResultMap">
						SELECT ADMIN_ID,
				       NVL(ADMIN_PWD, '') ADMIN_PWD,
				       NVL(ADMIN_NAME, '') ADMIN_NAME,
				       NVL(ADMIN_STAT, '') ADMIN_STAT,
				       NVL(TO_CHAR(REG_DT, 'YYYY.MM.DD HH24:MI:SS'), '') REG_DT
				FROM ADMIN
				WHERE ADMIN_ID = #{value}
	</select>
	
</mapper>
